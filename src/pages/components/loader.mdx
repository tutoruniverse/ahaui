---
title: Loader
---
import { graphql } from 'gatsby';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';
import sizesLoader from 'src/examples/Loader/size';

# Loader

## Introduction

Loaders are used to indicate that users have triggered an action but the result has not been completely loaded yet.

## Examples

### Basic

A simple loading status
<CodeView codeText={'<Loader/>'} />

### Sizes
<CodeView codeText={sizesLoader} />

### Custom animation duration

<CodeView codeText={'<Loader duration={500} />'} />

## Design Reference
N/A

## API

<ComponentApi metadata={props.data.metadata} />


export const query = graphql`
  query LoaderQuery {
    metadata: componentMetadata(displayName: { eq: "Loader" }) {
      displayName
      ...ComponentApi_metadata
    }
  }
`;
