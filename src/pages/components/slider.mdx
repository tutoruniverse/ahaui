---
title: Slider
---

import { graphql } from 'gatsby';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';

import sliderDefaultWithTooltip from 'src/examples/Slider/defaultWithTooltip';
import sliderWithFixedValues from 'src/examples/Slider/fixedValues';
import sliderRange from 'src/examples/Slider/range';
import sliderVariants from 'src/examples/Slider/variant';

# Slider

## Introduction

The slider is an interactive component that lets the user control input with a sliding axis to choose a single value or range between min and max values.

It's often used in bidding screens for Experts setting the bid amount.

Base on [rc-slider][1], modified default style.

<img
  className="u-maxWidthFull"
  src={require('src/assets/structure/stb_slider-structure.jpg')}
  srcSet={`${require('src/assets/structure/stb_slider-structure@2x.jpg')} 2x`}
  alt="Slider structure"
/>
<ol className="u-paddingLeftLarge u-textGray u-marginTopTiny">
  <li>
      Range
  </li>
  <li>
      Slider
  </li>
  <li>
      Handle
  </li>
  <li>
      Tooltip&nbsp;
    <i className="u-text200">- Used to displays the value number.</i>
  </li>
</ol>

## Examples

### Default with tooltip

<CodeView codeText={sliderDefaultWithTooltip} />

### Slider with fixed values

<CodeView codeText={sliderWithFixedValues} />

### Range with custom tooltip

<CodeView codeText={sliderRange} />

### Colors

<CodeView codeText={sliderVariants} />

## Design Reference

Using a tooltip to show the value number instead of putting the value number in the handle.

<div className="Grid u-marginBottomLarge">
  <div className="u-sizeFull md:u-size1of2">
    <img
      className="u-maxWidthFull"
      src={require('src/assets/ref/slider-structure-do.jpg')}
      srcSet={`${require('src/assets/ref/slider-structure-do@2x.jpg')} 2x`}
      alt="Slider Do"
    />
    <div className="u-borderBottom u-borderLarge u-borderPositive u-marginTopSmall" />
    <div className="u-textPositive">Do</div>
  </div>
  <div className="u-sizeFull md:u-size1of2">
    <img
      className="u-maxWidthFull"
      src={require('src/assets/ref/slider-structure-dont.jpg')}
      srcSet={`${require('src/assets/ref/slider-structure-dont@2x.jpg')} 2x`}
      alt="Slider Dont"
    />
    <div className="u-borderBottom u-borderLarge u-borderNegative u-marginTopSmall" />
    <div className="u-textNegative">Don't</div>
  </div>
</div>

## API

<ComponentApi metadata={props.data.metadata} />

[1]: https://www.npmjs.com/package/rc-slider

export const query = graphql`
  query SliderQuery {
    metadata: componentMetadata(displayName: { eq: "Slider" }) {
      ...ComponentApi_metadata
    }
  }
`;
