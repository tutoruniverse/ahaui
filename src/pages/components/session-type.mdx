---
title: Session Type
---

import { graphql } from 'gatsby';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';

import variantsSessionType from 'src/examples/SessionType/variants';
import withTooltipSessionType from 'src/examples/SessionType/withTooltip';

# Session Type

## Introduction

The component helps users (Experts) to know which type of session they are working on. It's currently used in Excelchat only.

<img
  src={require('src/assets/structure/stb_session-types-structure.jpg')}
  srcSet={`${require('src/assets/structure/stb_session-types-structure@2x.jpg')} 2x`}
  className="u-maxWidthFull"
  alt="Session Types"
/>
<ol className="u-textGray u-marginTopTiny">
  <li>
    Icon
    <i className="u-text200">
      -  An information icon which helps users know that this is a helping object.
    </i>
  </li>
  <li>
    Label
    <i className="u-text200">
      -  Displays session types.
    </i>
  </li>
  <li>
    CTA
    <i className="u-text200">
      - Opens the session type guides.
    </i>
  </li>
  <li>
    Background
    <i className="u-text200">
      - Different session types have different background colors, increase in contrast to show an increase in the importance of the guides respectively: User Session -> Demo Session -> Mentoring Session -> Learning Session.
    </i>
  </li>
</ol>

## Examples

### Types

The session types are proportionate with these colors:

<CodeView exampleClassName="example-sessionType" codeText={variantsSessionType} />

### With tooltip

<CodeView codeText={withTooltipSessionType} />

## Design Reference

N/A


## API

<ComponentApi metadata={props.data.metadata} />


export const query = graphql`
  query SessionTypeQuery {
    metadata: componentMetadata(displayName: { eq: "SessionType" }) {
      ...ComponentApi_metadata
    }
  }
`;
