---
title: Progress
---

import { graphql } from 'gatsby';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';

import groupProgress from 'src/examples/Progress/group';
import variantsProgress from 'src/examples/Progress/variants';

# Progress

## Introduction

The component is used to help users to indicate the current state; and guide them through a set of steps in a specific process.

<img
  src={require('src/assets/structure/stb_progress-structure.jpg')}
  srcSet={`${require('src/assets/structure/stb_progress-structure@2x.jpg')} 2x`}
  alt="Progress structure"
/>
<ol className="u-textGray u-marginTopTiny">
  <li>
      Indicator
    <i className="u-text200"> - An animated or fixed bar which is used to indicate the current state of the process.</i>
  </li>
  <li>
      Label
    <i className="u-text200"> - A text object, which is used to shortly describe the current state (i.e. percentage).</i>
  </li>
  <li>
      Progress bar
    <i className="u-text200">
        - A fixed-width bar, with outlined or secondary colors, that indicates the whole process.
    </i>
  </li>

</ol>

## Examples

### Types

#### Default

<CodeView codeText={'<Progress now={35} />'} />

#### With label

The progress with label’s `height` should be at least `16px`.

<CodeView codeText={'<Progress now={35} label="35%" height={16} />'} />

#### Striped

<CodeView codeText={'<Progress striped now={35} />'} />

#### Animated

<CodeView codeText={'<Progress animated striped now={35} />'} />

#### Border

<CodeView codeText={'<Progress now={65} border />'} />

### Progress group

<CodeView codeText={groupProgress} />

### Variants

<CodeView exampleClassName="example-progress" codeText={variantsProgress} />

## Design Reference

N/A


## API

<ComponentApi metadata={props.data.metadata} />


export const query = graphql`
  query ProgressQuery {
    metadata: componentMetadata(displayName: { eq: "Progress" }) {
      ...ComponentApi_metadata
    }
  }
`;
