---
title: Separator
---
import { graphql } from 'gatsby';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';
import separatorColor from 'src/examples/Separator/color';
import separatorLineType from 'src/examples/Separator/lineType';

# Separator

## Introduction

A divider that separates two different sets of content.

<img
  src={require('src/assets/structure/stb_separator-structure.jpg')}
  srcSet={`${require('src/assets/structure/stb_separator-structure@2x.jpg')} 2x`}
  className="u-maxWidthFull"
  alt="Separator"
/>
<ol className="u-textGray u-marginTopTiny">
  <li>
    Label
  </li>
  <li>
    Left line
  </li>
  <li>Right line</li>
</ol>

## Examples

### Default

A solid Separator without a label.
<CodeView codeText={'<Separator  />'} />

### With label

A dashed Separator with label center-aligned.

<CodeView codeText={'<Separator label="Label" />'} />

### Colors

<CodeView codeText={separatorColor} />

### Line styles

<CodeView codeText={separatorLineType} />

## Design Reference

N/A

## API

<ComponentApi metadata={props.data.metadata} />


export const query = graphql`
  query SeparatorQuery {
    metadata: componentMetadata(displayName: { eq: "Separator" }) {
      displayName
      ...ComponentApi_metadata
    }
  }
`;
