---
title: Logo
---
import { graphql } from 'gatsby';
import { useState } from 'react';
import { Logo,SafeAnchor } from '@ahaui/react';
import ComponentApi from 'src/components/ComponentApi';
import CodeView from 'src/components/CodeView';

# Logo

## Examples

<CodeView codeText={`<Logo src="https://raw.githubusercontent.com/gotitinc/aha-assets/master/origin/ahaui-logo-with-text.svg" />`} />

## API

<ComponentApi metadata={props.data.metadata} />


export const query = graphql`
  query LogoQuery {
    metadata: componentMetadata(displayName: { eq: "Logo" }) {
      displayName
      ...ComponentApi_metadata
    }
  }
`;

