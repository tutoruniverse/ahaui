---
title: What's New?
---

import { graphql } from "gatsby";
import ReactMD from "react-markdown";

# Release Notes

## CHANGELOG React

<div className="ReleaseNotes">
  <ReactMD
    source={
      props.data.allFile.nodes[0].childrenMarkdownRemark[0].rawMarkdownBody
    }
  />
</div>
<hr />

## CHANGELOG CSS

<div className="ReleaseNotes">
  <ReactMD
    source={
      props.data.allFile.nodes[1].childrenMarkdownRemark[0].rawMarkdownBody
    }
  />
</div>

export const pageQuery = graphql`
  query ChangeLog {
    allFile(filter: { absolutePath: { regex: "//CHANGELOG/" } }) {
      nodes {
        childrenMarkdownRemark {
          rawMarkdownBody
        }
      }
    }
  }
`;
