---
title: What's New?
---

import { graphql } from "gatsby";
import ReactMD from "react-markdown";

# Release Notes

## CHANGELOG React

<ReactMD
  source={props.data.allFile.nodes[0].childrenMarkdownRemark[0].rawMarkdownBody}
/>
<hr/>

## CHANGELOG CSS

<ReactMD
  source={props.data.allFile.nodes[1].childrenMarkdownRemark[0].rawMarkdownBody}
/>

export const pageQuery = graphql`
  query ChangeLog {
    allFile(filter: { absolutePath: { regex: "//CHANGELOG/" } }) {
      nodes {
        childrenMarkdownRemark {
          rawMarkdownBody
        }
      }
    }
  }
`;
